const fetch = require('node-fetch'); const urls = ['/api/discord/stats','/api/discord/roles','/api/discord/members','/api/website/stats']; (async () => { for (const url of urls) { try { const res = await fetch('http://localhost:3000' + url, { cache: 'no-store' }); console.log(url, res.status, res.statusText); const body = await res.text(); console.log(body.slice(0, 500)); } catch (err) { console.error(url, 'ERROR', err.message); } } })();
